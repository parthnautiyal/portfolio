spring.application.name=portfolio-backend
server.port=8080

# CORS — override via CORS_ALLOWED_ORIGINS env var in production
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:4200,http://localhost:5173}
